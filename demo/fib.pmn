(:= fib (fn (n)
    (:= a 0)
    (:= b 1)
    (:= i 0)
    (while (!= n i)
        (:= tmp a)
        (= a b)
        (= b (+ tmp b))
        (= i (+ i 1)))
    a))

(print "Fibonacci number 7 is" (fib 7))
(print "Fibonacci number 10 is" (fib 10))
